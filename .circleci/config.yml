version: 2.1

executors:
  machine-executor:
    machine:
      image: ubuntu-2004:202101-01

jobs:
  install:
    executor: machine-executor
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "package.json" }}
            - v1-dependencies-
      - run:
          name: Install dependencies
          command: npm install
      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "package.json" }}

  run-cypress-tests:
    executor: machine-executor
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "package.json" }}
            - v1-dependencies-
      - run:
          name: Install Cypress dependencies
          command: npm install cypress --save-dev
      - run:
          name: Run Cypress tests
          command: npx cypress run --spec "cypress/e2e/ui-tests/login.feature"
      - store_artifacts:
          path: cypress/screenshots
          destination: cypress/screenshots
      - store_test_results:
          path: cypress/results

workflows:
  version: 2
  test:
    jobs:
      - install
      - run-cypress-tests:
          requires:
            - install

